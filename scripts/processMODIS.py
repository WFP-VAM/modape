#!/usr/bin/env python

def main():
    pass

    # ppatt = re.compile(r'M\w{6}')
    # tpatt = re.compile(r'h\d+v\d+\.\d{3}')
    # grps = list(set(['-'.join(re.findall(ppatt,os.path.basename(x)) + re.findall(tpatt,os.path.basename(x)))  for x in fls]))
    # fls_sub = [x for x in fls if g.split('-')[0] in x and g.split('-')[1] in x]


if __name__ == '__main__':
    main()
